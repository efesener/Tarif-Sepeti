<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tarifsepeti">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TarifSepeti">
        <activity android:name=".TarifAl" android:label="Tarif Önerisi Al"></activity>
        <activity android:name=".TarifEkle" android:label="Tarif Ekle" />
        <activity android:name=".TarifDetay" android:label="Tarif Detay"/>
        <activity android:name=".TarifListele" android:label="Tariflerim" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="Tarif Sepeti Giriş/Kayıt">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>